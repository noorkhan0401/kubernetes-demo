apiVersion: v1
kind: Service
metadata:
  name: svc-kubetrain
spec:
  type: LoadBalancer
  selector:
    tag: label-kubetrain
  ports:
  - name: port-lb-kubetrain
    protocol: TCP
	port: 8080 #this is the service port
	targetPort: 80 #this is the container port